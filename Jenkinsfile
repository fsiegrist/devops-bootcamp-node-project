#!/usr/bin/env groovy

pipeline {
    agent any
    stages {
        stage("Bump Version") {
            steps {
                script {
                    echo 'incrementing patch version...'
                }
            }
        }
    }
}